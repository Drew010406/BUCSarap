import 'package:flutter/material.dart';

class OrderInfoModal extends StatelessWidget {
  const OrderInfoModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
