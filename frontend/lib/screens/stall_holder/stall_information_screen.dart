import 'package:flutter/material.dart';

class StallInformationScreen extends StatelessWidget {
  const StallInformationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
